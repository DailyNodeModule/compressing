This project tests out some of the features of [compressing](https://github.com/node-modules/compressing/) a compression library for node.js.

Run with `docker-compose run app` and check the `output` folder.